import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 10,
        borderRadius: 40,
        borderWidth: 3,
        borderColor: "#ae22e0",
        // height: 70, 
        // width: 70
    },
    image: {
        borderRadius: 40,
        borderWidth: 1,
        borderColor: "#ffffff"
    },
});
export default styles;